# Use an existing docker image as a base
FROM mongo:6.0.7

# Create a new user and group 'dbuser'
RUN groupadd -r dbuser && useradd --no-log-init -r -g dbuser dbuser

# Change the ownership of the directory to 'expressuser'
RUN mkdir -p /etc/certs && chown -R expressuser:expressuser /etc/certs

# Change the ownership of the directory to 'dbuser'
RUN chown -R dbuser:dbuser /etc/certs

# Switch to 'dbuser'
USER dbuser
